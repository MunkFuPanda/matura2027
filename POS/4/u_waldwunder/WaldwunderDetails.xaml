<Window x:Class="u_waldwunder.WaldwunderDetails"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:u_waldwunder"
        mc:Ignorable="d"
        Title="WaldwunderDetails" Height="600" Width="500">
    <Grid Margin="0,0,10,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Name" Margin="0,0,0,10" Grid.Row="0">
            <TextBlock x:Name="NameTextBlock" Margin="0,0,0,5" FontSize="16" FontWeight="Bold"/>
        </GroupBox>
        <GroupBox Header="Beschreibung" Margin="0,0,0,10" Grid.Row="1">
            <TextBlock x:Name="DescriptionTextBlock" Margin="0,0,0,5" FontSize="12" TextWrapping="Wrap"/>
        </GroupBox>
        <GroupBox Header="Bundesland" Margin="0,0,0,10" Grid.Row="2">
            <TextBlock x:Name="ProvinzTextBlock" Margin="0,0,0,5" FontSize="12" TextWrapping="Wrap"/>
        </GroupBox>
        <GroupBox Header="Art" Margin="0,0,0,10" Grid.Row="3">
            <TextBlock x:Name="TypeTextBlock" Margin="0,0,0,5" FontSize="12" TextWrapping="Wrap"/>
        </GroupBox>
        <GroupBox Header="Bilder" Margin="0,0,0,10" Grid.Row="4">
            <ListBox x:Name="ImageListBox" Margin="0,0,0,10" ScrollViewer.HorizontalScrollBarVisibility="Disabled" HorizontalContentAlignment="Stretch" SelectionMode="Single" ScrollViewer.VerticalScrollBarVisibility="Visible">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderThickness="1" BorderBrush="Gray" Padding="5" Margin="0,0,0,5">
                            <StackPanel Orientation="Vertical">
                                <Image Source="{Binding}" Stretch="Uniform" Margin="0,0,5,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>
    </Grid>
</Window>
