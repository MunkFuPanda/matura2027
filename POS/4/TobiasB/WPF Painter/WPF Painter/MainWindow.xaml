<Window x:Class="WPF_Painter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:painter="clr-namespace:Painter;assembly=Painter"
        xmlns:avalon="http://icsharpcode.net/sharpdevelop/avalonedit"
        xmlns:local="clr-namespace:WPF_Painter"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="2*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            <avalon:TextEditor Height="400" x:Name="tb_input" VerticalScrollBarVisibility="Visible" ShowLineNumbers="True" SyntaxHighlighting=""></avalon:TextEditor>
            <Button Content="Run" Click="Button_Click"></Button>
        </StackPanel>

        <painter:PainterControl x:Name="painterGrid" Grid.Column="1"></painter:PainterControl>

    </Grid>
</Window>
