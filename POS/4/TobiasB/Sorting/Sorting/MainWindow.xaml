<Window x:Class="Sorting.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:s="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:Sorting"
        Title="Sorting-Algorithmen" Height="350" Width="800">
    <DockPanel>
        <StackPanel Width="100">
            <Button Name="start" Margin="10" Click="start_Click">Sort</Button>
            <Button Name="reverse" Margin="10 0 10 10" Click="reverse_Click">Reverse</Button>
            <Button Name="cocktail" Margin="10 0 10 10" Click="cocktail_Click">Cocktail</Button>
            <Button Name="selection" Margin="10 0 10 10" Click="selection_Click">Selection</Button>
            <Button Name="insertion" Margin="10 0 10 10" Click="insertion_Click">Insertion</Button>
            <Button Name="comb" Margin="10 0 10 10" Click="comb_Click">Comb</Button>
            <Button Name="shell" Margin="10 0 10 10" Click="shell_Click">Shell</Button>
            <Button Name="heap" Margin="10 0 10 10" Click="heap_Click">Heap</Button>
            <Button Name="merge" Margin="10 0 10 10" Click="merge_Click">Merge</Button>
            <Button Name="quick" Margin="10 0 10 10" Click="quick_Click">Quick</Button>
            <TextBlock Margin="10 0 10 0">Checks:</TextBlock>
            <TextBlock Margin="20 0 0 10" Text="{Binding Path=Checks}"></TextBlock>
            <TextBlock Margin="10 0 10 0">Swaps:</TextBlock>
            <TextBlock Margin="20 0 0 10" Text="{Binding Path=Swaps}"></TextBlock>
        </StackPanel>
        <ListBox Name="sortingBox" ItemsSource="{Binding Path=List}" SelectedIndex="{Binding Path=Selected, Mode=OneWay}" DataContext="{Binding}">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type s:Int32}">
                    <ProgressBar Name="pBar" Minimum="0" Maximum="200" Value="{Binding Mode=OneWay}" Orientation="Vertical" Width="10" Height="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=StackPanel, AncestorLevel=1}, Path=ActualHeight}"></ProgressBar>
                    <DataTemplate.Triggers>
                        <DataTrigger
                            Binding="{Binding
                            RelativeSource={RelativeSource
                            Mode=FindAncestor,
                            AncestorType={x:Type ListBoxItem}},
                            Path=IsSelected}"
                            Value="True">
                        <Setter
                            TargetName="pBar"
                            Property="Background"
                            Value="Gold"/>
                        <Setter
                            TargetName="pBar"
                            Property="Foreground"
                            Value="Gold"/>
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
</Window>
