<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Wecker_Timer">
    <Style TargetType="{x:Type local:Timer}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:Timer}">
                    <Border Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <StackPanel>
                            <TextBlock Foreground="Red" FontSize="36" 
                               x:Name="PART_CURRENTDATETIME"></TextBlock>
                            <Button x:Name="PART_SETALARMBUTTON" >
                                <StackPanel>
                                    <TextBlock FontSize="24">Set Time</TextBlock>
                                    <TextBlock FontSize="24" x:Name="PART_SETALARMBUTTONTEXTPANE"></TextBlock>
                                </StackPanel>
                            </Button>
                            <Button x:Name="PART_STARTBUTTON">Start Timer</Button>
                            <Button x:Name="PART_PAUSEBUTTON">Pause Timer</Button>
                            <Button x:Name="PART_RESETBUTTON">Reset Timer</Button>
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
