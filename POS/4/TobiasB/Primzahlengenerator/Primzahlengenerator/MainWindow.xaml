<Window x:Class="Primzahlengenerator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Primzahlengenerator"
        mc:Ignorable="d"
        Title="Primzahlengenerator" Height="800" Width="800">
    
    
    <StackPanel>
        <Label Content="Welche Primzahl soll berechnet werden?"></Label>
        <TextBox x:Name="input"></TextBox>
        <Button x:Name="startButton" Click="startButton_Click">Start</Button>
        <Label x:Name="output"></Label>
        <Image Name="load_image" Stretch="Fill" Width="256" Height="256" Source="/sync.png" Visibility="Hidden">
            <Image.RenderTransform>
                <RotateTransform Angle="0" CenterX="128" CenterY="128" />
            </Image.RenderTransform>
        </Image>
        <DockPanel>
            <ListView x:Name="lv_prim" DockPanel.Dock="Top" MaxHeight="100"></ListView>
        </DockPanel>
        
        
    </StackPanel>
    <Window.Resources>
        <Storyboard x:Key="loadingRotation">
            <DoubleAnimation Storyboard.TargetName="load_image" Storyboard.TargetProperty="(Image.RenderTransform).(RotateTransform.Angle)" From="0" To="360" Duration="0:0:2.0" RepeatBehavior="Forever" />
        </Storyboard>
    </Window.Resources>
</Window>
