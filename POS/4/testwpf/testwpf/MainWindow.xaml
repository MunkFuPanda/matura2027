<Window x:Class="testwpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:testwpf"
        mc:Ignorable="d"
        AllowsTransparency="True"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        Title="SystemMonitor" Height="450" Width="800" WindowStyle="None" Background="Transparent">

    <Window.Resources>
        <tb:TaskbarIcon
        x:Key="TrayIcon"
        IconSource="/67.ico"
        ToolTipText="System Monitor">

            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Ein / Ausblenden"
                          Click="ToggleVisibility"/>
                    <Separator/>
                    <MenuItem Header="Beenden"
                          Click="ExitApp"/>
                    <MenuItem Header="Indicator anzeigen"
      Click="ShowIndicator"/>

                    <MenuItem Header="Graph anzeigen"
      Click="ShowGraph"/>

                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>

        </tb:TaskbarIcon>
    </Window.Resources>

    <Grid MouseLeftButtonDown="Grid_MouseLeftButtonDown">
        <TextBlock Text="CPU %" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,33" Background="LightGray" Width="300" TextAlignment="Center"/>
        <local:CustomControl1
        x:Name="indicator"
        Width="400"
        Height="400"
        CurrentValue="0"
        MinimumValue="0"
        MaximumValue="100"/>
    </Grid>
</Window>
